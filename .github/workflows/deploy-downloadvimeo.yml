name: Deploy downloadvimeo.com

on:
  push:
    branches: [ main ]
    paths:
      - 'sites/downloadvimeo.com/**'
      - 'packages/**'
      - '.github/actions/deploy-site/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/deploy-downloadvimeo.yml'
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        uses: ./.github/actions/deploy-site
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        with:
          pkg: "@apps/downloadvimeo.com"
          dir: "sites/downloadvimeo.com"
          repo: "serpcompany/downloadvimeo.com"
          branch: "gh-pages"
          cname: "downloadvimeo.com"
