name: Deploy wistiadownloader.com

on:
  push:
    branches: [ main ]
    paths:
      - 'sites/wistiadownloader.com/**'
      - 'packages/**'
      - '.github/actions/deploy-site/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/deploy-wistiadownloader.com.yml'
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        uses: ./.github/actions/deploy-site
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        with:
          pkg: "@apps/wistiadownloader.com"
          dir: "sites/wistiadownloader.com"
          repo: "serpcompany/wistiadownloader.com"
          branch: "gh-pages"
          cname: "wistiadownloader.com"
