name: Deploy skooldownloader.com

on:
  push:
    branches: [ main ]
    paths:
      - 'sites/skooldownloader.com/**'
      - 'packages/**'
      - '.github/actions/deploy-site/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/deploy-skooldownloader.yml'
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        uses: ./.github/actions/deploy-site
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        with:
          pkg: "@apps/skooldownloader.com"
          dir: "sites/skooldownloader.com"
          repo: "serpcompany/skooldownloader.com"
          branch: "gh-pages"
          cname: "skooldownloader.com"

